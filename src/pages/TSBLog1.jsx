// This file intentionally re-exports TSBLog as TSBLog1 so it appears correctly in the page list.
// The content lives in pages/TSBLog.jsx — this is the canonical entry point for /TSBLog1 route.
export { default } from './TSBLog';